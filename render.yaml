services:
  - type: web
    name: presidio-well-route-map
    runtime: static
    staticPublishPath: .
    buildCommand: echo "Static site - no build needed"
    routes:
      - type: rewrite
        source: /*
        destination: /well_map_v2_9.html
